version: "3.9"

services:
  api:
    image: bref/php-82-fpm-dev:2
    ports: [ '8000:8000' ]
    volumes:
      - .:/var/task:ro
      - ./var:/var/task/var
    environment:
      HANDLER: public/index.php