.env.local
.env.local.php
.env.*.local
config/secrets/prod/prod.decrypt.private.php
public/bundles/
var/
vendor/
.terraform/
phpunit.xml
.phpunit.cache
.phpcs-cache
.idea/
.localstack/
bigquery.json
github.pem

# Ignore lock files in packages, as library lock files have no effect on the application and do
# cause Dependabot to raise cyclical (and unnessesary) PRs for inter-dependant packages
#
# https://getcomposer.org/doc/02-libraries.md#lock-file
packages/**/composer.lock